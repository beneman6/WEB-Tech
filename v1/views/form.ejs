<!DOCTYPE html>
<html lang="de">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-width, initial-scale=1.0"
    />

    <title>WEB Tutorials</title>
    <link rel="icon" type="images/x-icon" href="img/html.svg" />
    <link rel="stylesheet" href="css/style.css" />
    <link rel="stylesheet" href="css/form.css" />
    <link rel="stylesheet" href="css/grid.css" />
  </head>

  <body>
    <%- include("includes/header.ejs") %>
    <%- include("includes/nav.ejs") %>
    <main>
      <div id="formular">
        <h2>Erstellen eines neuen Tutorials</h2>
        <form
          method="post"
          action="/form"
        >
          <fieldset>
            <legend>Daten</legend>
            <p>
              <label for="titel">Titel</label>
              <span class="inputSpan">
                <input
                  placeholder="Titel mit min. 4 Zeichen und Großbuchstaben"
                  name="name"
                  id="titel"
                  required
                  minlength="4"
                  maxlength="20"
                  pattern="[A-Z][a-z]*"
              /></span>
            </p>
            <p>
              Sprache

              <select name="language" id="sprache">
                <option value="-" id="keine">-</option>
                <option value="de" id="deutsch">Deutsch</option>
                <option value="en" id="englisch">Englisch</option>
                <option value="es" id="spanisch">Spanisch</option>
                <option value="fr" id="französisch">Französisch</option>
              </select>
            </p>

            <p>
              <label for="kategorien">Kategorie(n)</label>
              <input
                type="text"
                id="kategorien"
                placeholder="Kategorien getrennt durch Kommta"
                name="categories"
                pattern="\w+([,] \w+)*"
              />
            </p>
          </fieldset>
          <fieldset>
            <legend>Zusammenfassung</legend>
            <p>
              <label for="beschreibung">Beschreibung</label>
              <textarea
                id="beschreibung"
                placeholder="Beschreibungstext"
                name="description"
                minlength="40"
                cols="60"
                rows="4"
              ></textarea>
            </p>
            <p>
              <label for="dauer">Dauer</label>
              <input
                id="dauer"
                required
                name="duration"
                type="time"
                min="00:00"
                max="15:59"
              />
            </p>
            <p>
              <label for="veröffentlichung">Veröffentlichung (Datum)</label>
              <input id="veröffentlichung" name="date" type="date" required />
            </p>
          </fieldset>
          <fieldset>
            <legend>Inhalt</legend>

            <p>
              <label for="inhalt"> Inhalt</label>
              <input
                type="radio"
                value="text"
                name="type"
                required
                id="text"
              /><label for="text"> Text</label>
              <input
                type="radio"
                value="video"
                name="type"
                required
                id="video"
              />
              <label for="video"> Video</label>
            </p>
            <p>
              <input
                required
                name="content"
                placeholder="Der Inhalt des Tutorials"
                id="inhalt"
              />
            </p>

            <p><input type="submit" /></p>
          </fieldset>
        </form>
      </div>
      <%- include("includes/zusatz.ejs")%>
    </main>
    <%- include ("includes/footer.ejs") %>
    <script src="js/form.js"></script>
    <script src="js/burger.js"></script>
  </body>
</html>